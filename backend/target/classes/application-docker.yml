# ── Docker profile – overrides application.yml when SPRING_PROFILES_ACTIVE=docker ──
spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    com.documind: INFO
